 <% if logged_in?(:site_admin) %>
  <nav class="breadcrumb blogcrumb">
    <%= link_to fa_icon('file-text'), toggle_status_blog_path(blog), class: "primary", style: blog_status_color(blog)%>|
    <%= link_to fa_icon('pencil-square-o'), edit_blog_path(blog), class: "edit" %>|
    <%= link_to fa_icon('trash'), blog, method: :delete, data: { confirm: 'Are you sure?' }, class: "trash" %>
  </nav>
<%end%>


   